# HaRu 2.2.0

本版本修复 Rule34Video 多处分页协议不一致导致的媒体库和搜索结果只能读取第一页的问题。

## Rule34Video 分页

- 播放列表详情后续页改用站点的 `playlist_view_playlist_view` 异步区块协议；
- 上传者视频后续页改用 `list_videos_uploaded_videos` 异步区块协议；
- 搜索结果使用站点要求的合并分页参数，视频和相册会继续读取后续页；
- 分类、艺术家和播放列表订阅内容使用对应的异步区块分页协议。
